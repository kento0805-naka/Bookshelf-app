<%= form_with(url: add_read_book_path, local: true, method: :post) do |f|%>
  <%= f.hidden_field :title, value: book.title %>
  <%= f.hidden_field :author, value: book.author %>
  <%= f.hidden_field :image_url, value: book.image_url %>
  <%= f.hidden_field :item_url, value: book.item_url %>
  <% unless current_user.books.find_by(item_url: book.item_url) %>
    <%= f.submit '読了', class: 'btn btn-primary add_btn' %>
  <% end %>
<% end %>


<!--ステータスをつけて保存したい-->